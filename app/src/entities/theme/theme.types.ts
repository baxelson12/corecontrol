/** The two UI color themes. */
export type ThemeName = 'light' | 'dark';

/** What the user picked on the settings page: a theme, or follow the OS. */
export type ThemePreference = ThemeName | 'system';
